{
  "name": "root",
  "private": true,
  "scripts": {
    "server": "lerna run start --stream --parallel",
    "client": "lerna run serve --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
