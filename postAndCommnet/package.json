{
  "name": "root",
  "private": true,
  "scripts": {
    "tsc": "tsc",
    "start:tsc": "lerna run start:tsc --parallel",
    "start:nodemon": "lerna run start:nodemon --stream --parallel",
    "start:client": "lerna run serve --stream --parallel",
    "start": "concurrently npm:start:*"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "lerna": "^3.22.0",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "ts-node": "^8.10.1"
  }
}
