{
  "name": "root",
  "private": true,
  "scripts": {
    "server": "lerna run start --stream --parallel",
    "client": "lerna run serve --stream --parallel"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "lerna": "^3.22.0",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "ts-node": "^8.10.1"
  }
}
